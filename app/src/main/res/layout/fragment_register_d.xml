<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RadioGroup
        android:id="@+id/sexRadioGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.498">

        <RadioButton
            android:id="@+id/manButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="15dp"
            android:button="@drawable/ic_sex_man_selector"
            android:checked="true" />

        <RadioButton
            android:id="@+id/womanButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="15dp"
            android:button="@drawable/ic_sex_woman_selector"
            android:checked="false" />

    </RadioGroup>

    <com.beardedhen.androidbootstrap.BootstrapButton
        android:id="@+id/nextButton"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="下一步"
        app:bootstrapBrand="danger"
        app:bootstrapSize="xl"
        app:layout_constraintLeft_toLeftOf="@+id/sexRadioGroup"
        app:layout_constraintRight_toRightOf="@+id/sexRadioGroup"
        app:layout_constraintTop_toBottomOf="@+id/sexRadioGroup"
        app:roundedCorners="true" />
</android.support.constraint.ConstraintLayout>

